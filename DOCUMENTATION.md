# 🎯 Manda-AI 프로젝트 기술 문서

**최종 업데이트**: 2025-12-26  
**프로젝트 레포지토리**: https://github.com/koreadeveloper/manda

---

## 1. 프로젝트 개요

### 🎯 목적과 해결하려는 문제

**Manda-AI**는 AI를 활용하여 **만다라트(Mandal-Art) 목표 설정표**를 자동으로 생성해주는 웹 애플리케이션입니다.

기존 만다라트 작성의 문제점:
- 64개의 세부 실행 계획을 직접 작성하는 데 많은 시간 소요
- 체계적인 계획 수립 방법을 모르는 사용자에게 진입 장벽 존재
- 종이나 엑셀 기반 작성 시 수정/공유가 불편

**Manda-AI의 해결책**:
- 최종 목표 하나만 입력하면 AI가 8개 하위 목표 + 64개 실행 계획을 5~10초 내 자동 생성
- 인터랙티브 웹 UI로 실시간 편집 및 진행률 추적
- PNG 이미지 다운로드 및 URL 공유 기능

### 📝 한 줄 요약

> "AI가 당신의 꿈을 64개의 구체적인 실행 계획으로 변환합니다."

### 🚀 엘리베이터 피치 (30초 버전)

"목표는 있는데 어디서부터 시작해야 할지 모르시겠나요? Manda-AI에 최종 목표를 입력하세요. AI가 오타니 쇼헤이가 사용한 만다라트 기법으로 8개의 핵심 영역과 64개의 구체적인 실행 계획을 자동으로 만들어 드립니다. 수정도, 다운로드도, 공유도 원클릭!"

---

## 2. 전체 시스템 구조

### 📊 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                        사용자 (Browser)                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Vercel Edge Network                          │
│                  (호스팅 + 환경 변수 주입)                        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    React + Vite SPA                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │   App.tsx   │  │  Zustand    │  │  groqService.ts         │ │
│  │  (메인 UI)   │◄─│  (상태관리)  │◄─│  (AI API 호출)          │ │
│  └─────────────┘  └─────────────┘  └───────────┬─────────────┘ │
└────────────────────────────────────────────────┼───────────────┘
                                                 │
                                                 ▼
                               ┌─────────────────────────────────┐
                               │      Groq Cloud API             │
                               │   (Llama 3.3 70B Versatile)     │
                               └─────────────────────────────────┘
```

### 🔄 데이터 흐름 (Step-by-Step)

1. **사용자 입력**: "2025년 마라톤 완주" → AI 생성 버튼 클릭
2. **API 호출**: `groqService.ts`가 Groq API에 프롬프트 전송
3. **AI 처리**: Llama 3.3 모델이 JSON 형식으로 만다라트 생성
4. **응답 필터링**: 한자/일본어 등 비허용 문자 자동 제거
5. **상태 업데이트**: Zustand Store에 데이터 저장
6. **UI 렌더링**: 9×9 그리드에 실시간 반영
7. **로컬 저장**: `localStorage`에 자동 영속화

---

## 3. 기술 스택

| 영역 | 기술 | 선택 이유 |
|------|------|----------|
| **프레임워크** | React 19 | 컴포넌트 기반 UI, 대규모 커뮤니티, 풍부한 생태계 |
| **빌드 도구** | Vite | 빠른 HMR, 간편한 설정, 번들 최적화 |
| **언어** | TypeScript | 타입 안전성, IDE 지원, 유지보수성 |
| **상태 관리** | Zustand | Redux 대비 보일러플레이트 최소화, persist 미들웨어 내장 |
| **스타일링** | Tailwind CSS | 유틸리티 우선, 빠른 개발 속도, 반응형 용이 |
| **아이콘** | Lucide React | 가볍고 일관된 디자인, React 최적화 |
| **AI 서비스** | Groq API | 초고속 추론 (Llama 3.3), OpenAI 호환 규격 |
| **이미지 캡처** | html2canvas | 클라이언트 사이드 스크린샷, 서버 불필요 |
| **배포** | Vercel | Git 연동 자동 배포, 환경 변수 관리, Edge 배포 |

### ⚖️ 기술 선택 트레이드오프

- **Groq vs OpenAI**: Groq이 더 빠르고 비용 효율적이나, 모델 다양성은 OpenAI가 우세
- **Zustand vs Redux**: 소규모 프로젝트에 적합한 Zustand 선택 (Redux는 과도한 보일러플레이트)
- **Vite vs Next.js**: SSR/SSG 불필요, SPA로 충분하므로 가벼운 Vite 선택

---

## 4. 주요 기능 설명

### 4.1 🤖 AI 자동 생성

**사용자 관점**:
1. "AI 자동 생성" 버튼 클릭
2. 모달에서 최종 목표 입력 (예: "10kg 감량")
3. 5~10초 대기 후 전체 만다라트 자동 완성

**시스템 관점**:
```typescript
// groqService.ts
const response = await fetch(GROQ_API_URL, {
  body: JSON.stringify({
    model: "llama-3.3-70b-versatile",
    response_format: { type: "json_object" },
    messages: [{ role: "system", content: systemPrompt }, ...]
  })
});
```

**예외 처리**:
- 429 (Rate Limit): "서버 응답이 지연되고 있습니다. 잠시 후 다시 시도해 주세요."
- 400 (Model Error): "서버 모델 업데이트 중입니다."
- JSON 파싱 실패: "AI 응답을 파싱할 수 없습니다."

### 4.2 📊 9×9 인터랙티브 그리드

**구조**:
- 중앙: 최종 목표 (MM)
- 주변 8개 서브그리드: 각각 하위 목표 1개 + 실행 계획 8개

**실시간 동기화**:
- 중앙 그리드의 하위 목표 수정 시 → 해당 서브그리드 중앙 셀 자동 업데이트
- Zustand의 `syncFromCenter()` 함수가 양방향 동기화 담당

### 4.3 💾 다운로드 & 공유

**PNG 다운로드**:
```typescript
// html2canvas로 그리드 캡처
const canvas = await html2canvas(gridRef.current, {
  scale: 2, // 고해상도
  onclone: (doc) => {
    // textarea → div 변환 (줄바꿈 보존)
    doc.querySelectorAll('textarea').forEach(ta => {
      const div = doc.createElement('div');
      div.textContent = ta.value;
      ta.replaceWith(div);
    });
  }
});
```

**URL 공유**:
- 만다라트 데이터를 Base64로 인코딩하여 URL 파라미터에 포함
- 링크 공유 시 동일한 만다라트 복원 가능

---

## 5. 데이터 구조

### 📦 Zustand Store (store.ts)

```typescript
interface MandalartState {
  mainTitle: string;          // 최종 목표 텍스트
  theme: string;              // 테마 색상 (cyan, rose, etc.)
  isDarkMode: boolean;        // 다크 모드 여부
  subGrids: Record<GridPosition, MandaSubGrid>; // 9개 서브그리드
}

interface MandaSubGrid {
  position: GridPosition;     // 'MM' | 'TL' | 'TC' | ...
  cells: MandaCell[];         // 9개 셀 배열
}

interface MandaCell {
  text: string;               // 셀 텍스트
  completed: boolean;         // 완료 여부
  priority: 'none' | 'low' | 'medium' | 'high';
  isMainGoal?: boolean;       // 최종 목표 셀 여부
  isSubGoal?: boolean;        // 하위 목표 셀 여부
}
```

### 🔄 데이터 생성/저장/조회/삭제 흐름

| 동작 | 트리거 | 처리 |
|------|--------|------|
| **생성** | AI 생성 완료 | `setEntireManda()` 호출 → Store 전체 업데이트 |
| **저장** | 모든 상태 변경 | Zustand persist 미들웨어 → localStorage 자동 저장 |
| **조회** | 앱 로드 시 | localStorage에서 자동 복원 |
| **삭제** | 브라우저 캐시 삭제 | localStorage 데이터 삭제됨 |

---

## 6. 외부 서비스 연동

### 🤖 Groq API

| 항목 | 내용 |
|------|------|
| **목적** | 만다라트 자동 생성 (LLM 추론) |
| **엔드포인트** | `https://api.groq.com/openai/v1/chat/completions` |
| **모델** | `llama-3.3-70b-versatile` |
| **인증** | Bearer Token (`GROQ_API_KEY`) |
| **응답 형식** | JSON Object Mode |

**사용 시 주의점**:
- 무료 티어는 분당/일당 요청 제한(Rate Limit) 존재
- 모델 단종 시 400 에러 발생 가능 → 모델명 업데이트 필요
- 응답에 비한국어(한자, 일본어) 포함 가능 → 필터링 로직 적용 중

---

## 7. 보안 및 권한 관리

### 🔐 인증/인가

- **현재**: 인증 시스템 없음 (모든 사용자 익명 접근 가능)
- **API 키**: 서버 측(Vercel 환경 변수)에서만 관리, 클라이언트 노출 없음

### 🔒 민감 정보 관리

| 파일 | 목적 | Git 포함 여부 |
|------|------|--------------|
| `.env.local` | 로컬 개발용 API 키 | ❌ (.gitignore에 포함) |
| `.env.example` | 환경 변수 템플릿 | ✅ |
| Vercel 환경 변수 | 프로덕션 API 키 | ❌ (Vercel 대시보드에서 관리) |

### ⚠️ 보안 주의사항

> [!WARNING]
> 현재 API 키가 클라이언트 빌드에 포함되어 브라우저 개발자 도구에서 노출될 수 있습니다.
> 
> **권장 해결책**: API Route(서버리스 함수)를 통해 API 호출을 프록시하여 키 노출 방지

---

## 8. 배포 및 운영 구조

### 🌐 배포 환경

```
GitHub Repository (koreadeveloper/manda)
        │
        ▼ (자동 배포)
Vercel Project
        │
        ├── Production: https://manda.vercel.app
        └── Preview: PR별 자동 생성
```

### ⚙️ 환경 변수 관리

| 환경 | 변수명 | 설정 위치 |
|------|--------|----------|
| 로컬 개발 | `GROQ_API_KEY` | `.env.local` 파일 |
| 프로덕션 | `GROQ_API_KEY` | Vercel Dashboard → Settings → Environment Variables |

### 🚨 운영 중 발생 가능한 문제

| 문제 | 원인 | 대응 |
|------|------|------|
| AI 생성 실패 | Groq API 응답 지연/제한 | 재시도 안내 메시지 표시 |
| 모델 404 에러 | 모델 단종 | `groqService.ts`의 모델명 업데이트 |
| 이미지 다운로드 실패 | html2canvas 호환성 | 브라우저별 폴백 처리 |

---

## 9. 현재 상태

### ✅ 구현 완료

- [x] 9×9 인터랙티브 만다라트 그리드
- [x] Groq AI 기반 자동 생성
- [x] 다크/라이트 모드
- [x] 5가지 테마 컬러
- [x] 동적 폰트 스케일링
- [x] PNG 이미지 다운로드 (줄바꿈 지원)
- [x] URL 공유 기능
- [x] 로컬 저장 (localStorage)
- [x] 비한국어 문자 필터링
- [x] 반응형 디자인 (모바일/데스크톱)

### ⏳ 미구현/향후 구현 예정

- [ ] 사용자 인증 (로그인/회원가입)
- [ ] 클라우드 저장 (Supabase/Firebase)
- [ ] 여러 만다라트 관리
- [ ] PDF 다운로드
- [ ] 진행률 리마인더 알림

### 🐛 해결된 이슈

| 이슈 | 원인 | 해결 |
|------|------|------|
| Gemini 404 에러 | 모델명 변경 | Groq API로 전환 |
| 일본어/한자 출력 | LLM 특성 | 정규식 필터링 추가 |
| 이미지 줄바꿈 미반영 | textarea 렌더링 | div 변환 로직 추가 |
| 텍스트 잘림 | overflow: hidden | overflow: visible 변경 |

---

## 10. 향후 개선 및 확장 계획

### 🚀 기능 확장 아이디어

1. **사용자 인증 시스템**: Google/Kakao 소셜 로그인
2. **클라우드 동기화**: Supabase로 여러 기기에서 접근
3. **히스토리 관리**: 만다라트 버전 관리
4. **협업 기능**: 팀 만다라트 공동 편집
5. **템플릿 갤러리**: 인기 만다라트 템플릿 공유

### 🔧 구조 개선 포인트

1. **API Route 분리**: 현재 클라이언트에서 직접 Groq 호출 → Vercel API Route로 프록시
2. **컴포넌트 분리**: `App.tsx`가 과도하게 큼 → 헤더/푸터/모달 분리
3. **테스트 추가**: Vitest + React Testing Library

### 🧹 기술 부채

- `geminiService.ts` 파일 삭제 필요 (현재 미사용)
- 일부 인라인 스타일 → Tailwind 클래스로 통합
- 타입 정의 일부 `any` 사용 → 구체적 타입으로 교체

---

## 11. 인수인계를 위한 가이드

### 🖥️ 개발 환경 세팅

**필수 도구**:
- Node.js 18+
- npm 또는 yarn
- Git
- VS Code (권장)

**VS Code 권장 확장**:
- ESLint
- Prettier
- Tailwind CSS IntelliSense

### 🚀 로컬 실행 방법

```bash
# 1. 클론
git clone https://github.com/koreadeveloper/manda.git
cd manda

# 2. 의존성 설치
npm install

# 3. 환경 변수 설정
cp .env.example .env.local
# .env.local 열어서 GROQ_API_KEY 입력

# 4. 개발 서버 실행
npm run dev
# → http://localhost:3000 접속
```

### 📂 처음 볼 때 추천 순서

1. **`types.ts`** - 전체 데이터 구조 이해
2. **`store.ts`** - 상태 관리 로직 파악
3. **`App.tsx`** - 메인 UI 흐름 확인
4. **`groqService.ts`** - AI 연동 방식 이해
5. **`components/MandaCell.tsx`** - 개별 셀 동작 확인

### 🔑 핵심 파일 역할 요약

| 파일 | 역할 |
|------|------|
| `App.tsx` | 메인 레이아웃, 테마, 다운로드, 공유 |
| `store.ts` | Zustand 상태 관리, 데이터 영속화 |
| `groqService.ts` | Groq API 호출, 응답 파싱, 필터링 |
| `components/MandaCell.tsx` | 개별 셀 UI, 폰트 스케일링 |
| `components/SubGrid.tsx` | 3×3 서브그리드 구성 |
| `components/AIModal.tsx` | AI 생성 모달 UI |

---

## 📞 문의

- **GitHub Issues**: https://github.com/koreadeveloper/manda/issues
- **프로젝트 관리자**: koreadeveloper

---

> 이 문서는 Manda-AI 프로젝트의 전체적인 이해를 돕기 위해 작성되었습니다.
> 
> 추가 질문이나 수정 사항이 있으면 언제든 이슈를 등록해주세요!

